pipeline {
    agent any

    stages {

        stage('Test 1') {
            steps {
                echo "Jenkins is running fine"
            }
        }

        stage('List Files') {
            steps {
                sh "ls -al"
            }
        }

    }
}
